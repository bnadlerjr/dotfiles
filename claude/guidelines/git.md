# Git

## Commits

- **MUST NOT** reference Claude, Anthropic, or AI in commits — no
  "Co-Authored-By" lines, no "Claude suggested", no AI attribution of any kind
- For commit message formatting, use the `writing-git-commits` skill

## Branches

- Create feature branches: `feature/[description]`
- Never commit directly to main/master
